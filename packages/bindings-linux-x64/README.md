# @zvec/bindings-linux-x64

Platform-specific bindings for Zvec - a lightweight, lightning-fast, in-process vector database.

This package contains the native bindings for Linux on x64 (AMD64) architecture.
