# @zvec/bindings-darwin-x64

Platform-specific bindings for Zvec - a lightweight, lightning-fast, in-process vector database.

This package contains the native bindings for Darwin (macOS) on x64 (Intel) architecture.
