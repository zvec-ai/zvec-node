# @zvec/bindings-win32-x64

Platform-specific bindings for Zvec - a lightweight, lightning-fast, in-process vector database.

This package contains the native bindings for Windows on x64 (AMD64) architecture.
